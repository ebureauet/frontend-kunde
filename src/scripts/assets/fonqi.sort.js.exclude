// JavaScript Document
/// <reference path="jquery-1.4.2.min-vsdoc.js" />

$.fn.extend({
    customSort: function(sorter) {

        $(this).find("input[name='" + sorter + "]").each(function(index, element) {
            alert($(element).val());

        })

    }
});